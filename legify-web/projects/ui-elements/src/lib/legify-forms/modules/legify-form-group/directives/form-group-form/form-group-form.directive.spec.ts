import { FormGroupFormDirective } from './form-group-form.directive';

describe('FormGroupFormDirective', () => {
  it('should create an instance', () => {
    const directive = new FormGroupFormDirective();
    expect(directive).toBeTruthy();
  });
});
