import { FormSectionContentDirective } from './form-section-content.directive';

describe('FormSectionContentDirective', () => {
  it('should create an instance', () => {
    const directive = new FormSectionContentDirective();
    expect(directive).toBeTruthy();
  });
});
