import { FileUploadPreviewInvalidItemDirective } from './file-upload-preview-invalid-item.directive';

describe('FileUploadPreviewInvalidItemDirective', () => {
  it('should create an instance', () => {
    const directive = new FileUploadPreviewInvalidItemDirective();
    expect(directive).toBeTruthy();
  });
});
