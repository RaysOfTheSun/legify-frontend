import { FileUploadPreviewItemDirective } from './file-upload-preview-item.directive';

describe('FileUploadPreviewItemDirective', () => {
  it('should create an instance', () => {
    const directive = new FileUploadPreviewItemDirective();
    expect(directive).toBeTruthy();
  });
});
