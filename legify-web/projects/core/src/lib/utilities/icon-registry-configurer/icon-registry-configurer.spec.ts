import { IconRegistryConfigurer } from './icon-registry-configurer';

describe('IconRegistryConfigurer', () => {
  it('should create an instance', () => {
    expect(new IconRegistryConfigurer()).toBeTruthy();
  });
});
