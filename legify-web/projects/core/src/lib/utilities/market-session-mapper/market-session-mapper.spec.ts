import { MarketSessionMapper } from './market-session-mapper';

describe('MarketSessionMapper', () => {
  it('should create an instance', () => {
    expect(new MarketSessionMapper()).toBeTruthy();
  });
});
