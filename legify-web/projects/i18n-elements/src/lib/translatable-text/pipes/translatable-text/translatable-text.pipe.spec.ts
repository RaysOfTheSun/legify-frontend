import { TranslatableTextPipe } from './translatable-text.pipe';

describe('TranslatableTextPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslatableTextPipe();
    expect(pipe).toBeTruthy();
  });
});
